language: c
sudo: false
services:
  - docker
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-docker.sh
script: bash ./.travis-docker.sh
env:
 global:
   - PACKAGE="depextx"
   - POST_INSTALL_HOOK="opam install -y ocamlfind && opam config exec make && /repo/opam-depext --version && /repo/opam-depext -ui ssl && /repo/opam-depext -ui -j 2 -y -v pcre"
 matrix:
   - DISTRO=debian-stable OCAML_VERSION=4.00.1
   - DISTRO=debian-testing OCAML_VERSION=4.01.0
   - DISTRO=debian-unstable OCAML_VERSION=4.02.3
   - DISTRO=ubuntu-12.04 OCAML_VERSION=4.02.3
   - DISTRO=ubuntu-16.04 OCAML_VERSION=4.03.0
   - DISTRO=centos-6 OCAML_VERSION=4.01.0
   - DISTRO=centos-7 OCAML_VERSION=4.04.0
   - DISTRO=fedora-24 OCAML_VERSION=4.04.0
   - DISTRO=alpine OCAML_VERSION=4.05.0
